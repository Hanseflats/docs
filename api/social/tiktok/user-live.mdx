---
title: "Tiktok — User / Live"
sidebarTitle: "User / Live"
description: "Live"
---

Checks if a TikTok user is currently live streaming and retrieves their live room details. Returns `liveRoomUserInfo` (nickname, avatar, followerCount, roomId) and `liveRoom` (title, startTime, status, `liveRoomStats` with enterCount and userCount, plus `streamData` with playback URLs in multiple qualities).

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/tiktok/user/live
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `handle` | Yes | TikTok handle |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/tiktok/user/live?handle=example" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Tiktok](/api/social/tiktok).
