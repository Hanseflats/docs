---
title: "Tiktok — User / Showcase"
sidebarTitle: "User / Showcase"
description: "User Showcase"
---

Fetches products featured in a TikTok user's public showcase — the products a creator promotes on their profile. Returns an array of product objects each with title, price, images, and shop details. Use POST request if pagination is cutting off too early. Just send the query params in the body.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/tiktok/user/showcase
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `handle` | Yes | The handle of the user |
| `region` | No | Region to put the proxy in. Non-US TikTok Shop regions are not reliable right now and may return limited or inconsistent showcase data. Sorry for the inconvenience. |
| `cursor` | No | The cursor to the next page of products |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/tiktok/user/showcase?handle=example" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**2 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Tiktok](/api/social/tiktok).