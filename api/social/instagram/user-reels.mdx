---
title: "Instagram — User / Reels"
sidebarTitle: "User / Reels"
description: "Reels"
---

Returns a paginated list of a user's public Instagram reels (short-form videos). Each reel includes its shortcode, play count, like count, comment count, video versions with download URLs, thumbnail image, and owner info. Note that reel captions are not returned by this endpoint. Play counts are Instagram-only views and exclude cross-posted Facebook views. Supports cursor-based pagination via max_id; providing a user_id instead of a handle yields faster responses.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/instagram/user/reels
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `user_id` | No | Instagram user id. Use this for faster response times. |
| `handle` | No | Instagram handle. Use user_id for faster response times. |
| `max_id` | No | Max id to get more reels. Get 'max_id' from previous response. |
| `trim` | No | Set to true for a trimmed down version of the response |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/instagram/user/reels" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Instagram](/api/social/instagram).
