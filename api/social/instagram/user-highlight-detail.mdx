---
title: "Instagram — User / Highlight / Detail"
sidebarTitle: "User / Highlight / Detail"
description: "Highlights Details"
---

Fetches the full contents of a specific Instagram story highlight album by its ID. Returns the highlight's cover image, title, user info, and an items array containing each story with its media type, image or video URLs, dimensions, timestamp, and sticker/interactive element data. Useful for archiving or analyzing individual highlight reels.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/instagram/user/highlight/detail
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `id` | No | The ID of the highlight to get details for |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/instagram/user/highlight/detail" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**2 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Instagram](/api/social/instagram).