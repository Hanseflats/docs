---
title: "Instagram — User / Highlights"
sidebarTitle: "User / Highlights"
description: "Story Highlights"
---

Lists all story highlight albums for an Instagram user. Each highlight includes its ID, title, cover thumbnail URL, and owner info with username and profile picture. Accepts either a user_id or handle; providing user_id yields faster responses.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/instagram/user/highlights
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `user_id` | No | Instagram user id. Use for faster response times. |
| `handle` | No | Instagram handle. Use user_id for faster response times. |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/instagram/user/highlights" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Instagram](/api/social/instagram).
