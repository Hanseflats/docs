---
title: "X (Twitter) — Community / Tweets"
sidebarTitle: "Community / Tweets"
description: "Community Tweets"
---

Fetches tweets posted within a Twitter/X Community by URL. Returns an array of tweets, each with id, full_text, view_count, favorite_count, retweet_count, reply_count, bookmark_count, quote_count, created_at, and source. Each tweet includes a user object with the author's name, screen_name, avatar, followers_count, and is_blue_verified status.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/twitter/community/tweets
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `url` | Yes | Community URL |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/twitter/community/tweets?url=https://example.com" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [X (Twitter)](/api/social/twitter).
