---
title: "X (Twitter) — Community"
sidebarTitle: "Community"
description: "Community"
---

Retrieves details about a Twitter/X Community by URL. Returns the community name, description, rest_id, join_policy, created_at, member_count, rules, and creator_results with the creator's profile. Also includes members_facepile_results with avatar images of recent members.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/twitter/community
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `url` | Yes | Community URL |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/twitter/community?url=https://example.com" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [X (Twitter)](/api/social/twitter).
