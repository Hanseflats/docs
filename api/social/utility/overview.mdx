---
title: "Utility — Get Age and Gender"
sidebarTitle: "Get Age and Gender"
description: "Get Age and Gender"
---

Uses AI to analyze a creator's profile photo and estimate their age and gender. Returns ageRange with low and high bounds, gender, and a confidence score for the gender prediction. The profile photo must contain a clear, visible face for accurate results.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/detect-age-gender
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `url` | Yes | URL to users social profile |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/detect-age-gender?url=https://example.com" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Utility](/api/social/utility).
