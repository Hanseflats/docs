---
title: "Github — Repository"
sidebarTitle: "Repository"
description: "Repository"
---

Retrieves public metadata for one GitHub repository, including owner, description, language, stars, forks, topics, license, visibility, default branch, open issues, and timestamps. Pass a full GitHub repository url.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/github/repository
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `url` | Yes | GitHub repository URL |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/github/repository?url=https://example.com" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**2 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Github](/api/social/github).