---
title: "LinkedIn — Ad details"
sidebarTitle: "Ad details"
description: "Ad Details"
---

Retrieves detailed information about a specific LinkedIn ad by URL. Returns id, description, headline, adType, advertiser, and targeting with language, location, and audience criteria. Also includes totalImpressions, impressionsByCountry, adDuration, startDate, and endDate.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/linkedin/ad
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `url` | Yes | The url of the ad |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/linkedin/ad?url=https://example.com" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**2 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Linkedin](/api/social/linkedin).