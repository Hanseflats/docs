---
title: "Linkedin — Profile"
sidebarTitle: "Profile"
description: "Person's Profile"
---

Retrieves a person's public LinkedIn profile data, including their name, photo, location, follower count (followers), about/bio summary, recent posts, work experience, education, articles, activity feed, publications, projects, recommendations, and similar profiles. Only returns publicly available information visible in an incognito browser.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/linkedin/profile
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `url` | Yes | The URL of the LinkedIn profile to get |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/linkedin/profile?url=https://example.com" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Linkedin](/api/social/linkedin).
