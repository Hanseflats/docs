---
title: "Linkedin — Post"
sidebarTitle: "Post"
description: "Post"
---

Fetches a single LinkedIn post or article, returning the title, headline, full description text, author info with follower count, publication date, like count (reactions), comment count, and individual comments. Also includes related articles from the same author in moreArticles.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/linkedin/post
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `url` | Yes | The URL of the LinkedIn post to get |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/linkedin/post?url=https://example.com" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Linkedin](/api/social/linkedin).
