---
title: "Pinterest — User / Boards"
sidebarTitle: "User / Boards"
description: "User Boards"
---

Fetches a paginated list of boards for a Pinterest user, returning each board's name, url, description, pin\_count, follower\_count, owner info, cover\_images, and created\_at. Supports pagination via cursor and a trim option for lighter responses.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/pinterest/user/boards
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `handle` | Yes | The username of the user to get boards for. (e.g. broadstbullycom from [https://www.pinterest.com/broadstbullycom/](https://www.pinterest.com/broadstbullycom/)) |
| `trim` | No | Set to true for a trimmed down version of the response |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/pinterest/user/boards?handle=example" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**2 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Pinterest](/api/social/pinterest).