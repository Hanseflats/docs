---
title: "Facebook — Search ads"
sidebarTitle: "Search ads"
description: "Search"
---

Searches the Meta Ad Library by keyword and returns matching ads. Each result includes ad\_archive\_id, page\_name, is\_active, publisher\_platform, and a snapshot with body text, images, videos, and cta\_text. Results cap around 1,500 via GET due to cursor size limits; switch to POST method with body params for larger result sets.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/facebook/adLibrary/search/ads
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `query` | Yes | Keyword to search for |
| `sort_by` | No | Sort by impressions (high to low), or Most Recent (relevancy\_monthly\_grouped). Defaults to impressions. Enum: total\_impressions, relevancy\_monthly\_grouped. |
| `search_type` | No | If you want to search by exact phrase or not Enum: keyword\_unordered, keyword\_exact\_phrase. |
| `ad_type` | No | Search for all ads or only political and issue ads Enum: all, political\_and\_issue\_ads. |
| `country` | No | This can only be one country. It has to be the 2 letter code for the country. It defaults to ALL. |
| `status` | No | Status of the ad. Defaults to ACTIVE. Enum: ALL, ACTIVE, INACTIVE. |
| `media_type` | No | Media type of the ad. Defaults to ALL. Meme just means the ad has text and an image. No clue why they call it meme. Enum: ALL, IMAGE, VIDEO, MEME, IMAGE\_AND\_MEME, NONE. |
| `start_date` | No | Impressions start date. Needs to be in YYYY-MM-DD format. |
| `end_date` | No | Impressions end date. Needs to be in YYYY-MM-DD format. |
| `cursor` | No | Cursor to paginate through results |
| `trim` | No | Set to true for a trimmed down version of the response |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/facebook/adLibrary/search/ads?query=example" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**2 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Facebook](/api/social/facebook).