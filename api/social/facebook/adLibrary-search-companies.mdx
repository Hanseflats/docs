---
title: "Facebook — Adlibrary / Search / Companies"
sidebarTitle: "Adlibrary / Search / Companies"
description: "Search for Companies"
---

Searches for companies by name in the Meta Ad Library and returns their page IDs for use with other ad library endpoints. Each result includes page\_id, name, category, likes, verification status, and Instagram details like ig\_username and ig\_followers.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/facebook/adLibrary/search/companies
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `query` | Yes | Keyword to search for |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/facebook/adLibrary/search/companies?query=example" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**2 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Facebook](/api/social/facebook).