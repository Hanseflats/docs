---
title: "Facebook — Profile / Reels"
sidebarTitle: "Profile / Reels"
description: "Profile Reels"
---

Fetches up to 10 reels per request from a public Facebook page. Each reel includes id, url, view_count, description, creation_time, video_url, thumbnail, play_time_in_ms, and music details. Pagination requires passing both next_page_id and cursor from the previous response.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/facebook/profile/reels
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `url` | Yes | Facebook page URL |
| `next_page_id` | No | To paginate through to the next page |
| `cursor` | No | To paginate through to the next page |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/facebook/profile/reels?url=https://example.com" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Facebook](/api/social/facebook).
