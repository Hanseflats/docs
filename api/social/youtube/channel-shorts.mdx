---
title: "Youtube — Channel / Shorts"
sidebarTitle: "Channel / Shorts"
description: "Channel Shorts"
---

Retrieves a paginated list of short-form videos (Shorts) from a YouTube channel, including each short's title, URL, view count (views), likes, comments, and description. Supports sorting by newest or popular, and use the continuationToken to page through all results. Returns data in the shorts array.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/youtube/channel/shorts
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `handle` | No | Can pass channelId or handle |
| `channelId` | No | Can pass channelId or handle |
| `sort` | No | Sort by newest or popular Enum: newest, popular. |
| `continuationToken` | No | Continuation token to get more videos. Get 'continuationToken' from previous response. |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/youtube/channel/shorts" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Youtube](/api/social/youtube).
