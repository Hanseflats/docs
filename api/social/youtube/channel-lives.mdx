---
title: "Youtube — Channel / Lives"
sidebarTitle: "Channel / Lives"
description: "Channel Lives"
---

Fetches live streams and past streams from a YouTube channel's Live tab, including title, URL, thumbnail, view count, publish time, duration, and a continuationToken when YouTube has more results. Pass a handle or channelId for the first page, then pass continuationToken to page through more lives.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/youtube/channel/lives
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `channelId` | No | YouTube channel ID |
| `handle` | No | YouTube channel handle |
| `continuationToken` | No | Continuation token to get more lives. Get 'continuationToken' from previous response. |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/youtube/channel/lives" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**0.5 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Youtube](/api/social/youtube).
