---
title: "Youtube — Search"
sidebarTitle: "Search"
description: "Search"
---

Searches YouTube by keyword query and returns matching videos, channels, playlists, shorts, shelves, and live streams. Each video result includes title, URL, thumbnail, view count (views), publish date, duration, channel info, and badges. Supports filtering by upload date, sorting by relevance or popularity, and paginating with continuationToken. Set is\_paid\_promotions=true to search YouTube videos with the paid product placement / sponsorship disclosure.

## Endpoint

```text
GET https://app.lens.ly/api/v1/social/v1/youtube/search
```

Auth: `Authorization: Bearer YOUR_API_KEY` or `x-api-key`.

## Query parameters

| Parameter | Required | Description |
| --- | --- | --- |
| `query` | Yes | Search query |
| `uploadDate` | No | Upload date Enum: today, this\_week, this\_month, this\_year. |
| `sortBy` | No | Sort by Enum: relevance, popular. |
| `type` | No | Type of content to search for Enum: videos, shorts, channels, playlists. |
| `duration` | No | Duration of the video. Only applies to videos (not shorts). Enum: under\_3\_min, between\_3\_and\_20\_min, over\_20\_min. |
| `region` | No | 2 letter country code of the country to put the proxy in. |
| `continuationToken` | No | Continuation token to get more videos. Get 'continuationToken' from previous response. |
| `includeExtras` | No | This will get you the like \+ comment count and the description. To get the full details of the video, use the /v1/youtube/video endpoint. _This will slow down the response slightly._ |

## Example

```bash
curl "https://app.lens.ly/api/v1/social/v1/youtube/search?query=example" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

## Credits

**2 Lens credits** per upstream credit charged (~**2¢** typical). Cache hits are **free**.

See [Social API overview](/api/social) and [Youtube](/api/social/youtube).